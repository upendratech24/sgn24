import React from "react";
import ReactDOM from "react-dom";

const Login_Header = () => {
  return (
    <div className="Login_Header">{console.log("Login_Header Running...")}</div>
  );
};

export default Login_Header;
